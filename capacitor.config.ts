import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ztlemceny.tours',
  appName: 'Ztlemceny',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
